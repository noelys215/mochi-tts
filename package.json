{
  "name": "fish-study-reader",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "server"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "manual:fish": "npm --workspace server run manual:fish --",
    "proof": "npm test",
    "start": "npm --workspace server start",
    "test": "npm run test:unit && npm run test:browser",
    "test:unit": "npm --workspace server test && node --test tests/unit/*.test.js",
    "test:browser": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
